{% extends 'admin/base.html.twig' %}

{% block title %}{{title}}{% endblock %}

{% block body %}
    <section class="container-fluid">
        
        <div class="row justify-content-center">
          
            <div class="col-md-8 jumbotron mt-2">
                <h2 class="brown mb-2">Modification du plat n°{{meal.id}}</h2>
                <hr class="brown">

                {{form_start(form)}}

                <div class="row">
                    <div class="form-group mb-2">
                        {{form_label(form.title)}}
                        {{form_widget(form.title)}}
                    </div>
                    
                    <div class="form-group mb-2">
                        {{form_label(form.description)}}
                        {{form_widget(form.description)}}
                    </div>
                    
                    <div  class="form-group mb-4">
                        {{form_label(form.category)}}
                        {{form_widget(form.category)}}
                    </div>
                    

                    <div class="form-group row mb-2">
                        <div class="col mb-2">
                             <img src="{{meal.image}}" class="img-fluid" alt="{{meal.image}}"/>
                        </div>
                        <div class="col ">
                            {{form_label(form.image)}}
                            {{form_widget(form.image)}} 
                        </div>
                        
                    </div>
                   

                <div class="text-center">
                    <button type="submit" class="btn btn-brown"><i class="fa fa-check" aria-hidden="true"></i> Enregistrer</button>
                    <a href="{{path('admin_menus_list')}}" class="btn btn-light">Retour</a>
                </div>
                {{form_end(form)}}
            </div>
        </div>

    </section>

{% endblock %}